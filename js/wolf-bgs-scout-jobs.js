(() => {
  const host=document.querySelector('[data-scout-job-admin]'); if(!host)return;
  const API='/api/operations/scout-jobs',PAGE_SIZE=10;
  const list=host.querySelector('[data-scout-job-admin-list]'),defaultInput=host.querySelector('[data-scout-job-default]'),saveDefault=host.querySelector('[data-save-scout-job-default]'),resetDefault=host.querySelector('[data-reset-scout-job-default]'),refresh=host.querySelector('[data-refresh-scout-job-admin]'),status=host.querySelector('[data-scout-job-admin-status]'),search=host.querySelector('[data-scout-job-admin-search]'),count=host.querySelector('[data-scout-job-admin-count]'),pageLabel=host.querySelector('[data-scout-job-admin-page]'),prev=host.querySelector('[data-scout-job-admin-prev]'),next=host.querySelector('[data-scout-job-admin-next]');
  let payload=null,page=1;
  const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
  const fmtCredits=m=>Math.round((Number(m)||0)*1e6).toLocaleString()+' Cr';
  const norm=v=>String(v||'').trim().toLowerCase().replace(/\s+/g,' ');
  async function request(method='GET',body=null){const response=await fetch(API+(method==='GET'?'?admin=1&_='+Date.now():''),{method,credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json',...(body?{'Content-Type':'application/json','X-Mongrels-Request':'scout-jobs'}:{})},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Scout Job admin request failed ('+response.status+')');return data;}
  function ruleFor(system){const wanted=norm(system);for(const [name,row] of Object.entries(payload?.settings?.systems||{}))if(norm(name)===wanted)return row;return{enabled:true,bonusMillions:0,reason:'',bonusOnce:true};}
  function filteredJobs(){const q=norm(search?.value),jobs=Array.isArray(payload?.jobs)?payload.jobs:[];if(!q)return jobs;return jobs.filter(job=>{const rule=ruleFor(job.system);return[job.system,rule.reason,job.status].some(v=>norm(v).includes(q));});}
  function render(){if(!payload||!list)return;if(defaultInput&&document.activeElement!==defaultInput)defaultInput.value=String(Number(payload.defaultRewardMillions)||0);const jobs=filteredJobs(),pages=Math.max(1,Math.ceil(jobs.length/PAGE_SIZE));page=Math.min(Math.max(1,page),pages);const shown=jobs.slice((page-1)*PAGE_SIZE,page*PAGE_SIZE);if(count)count.textContent=jobs.length.toLocaleString()+' system'+(jobs.length===1?'':'s');if(pageLabel)pageLabel.textContent='Page '+page+' of '+pages;if(prev)prev.disabled=page<=1;if(next)next.disabled=page>=pages;if(!shown.length){list.innerHTML='<div class="scout-job-empty">No systems match this search.</div>';return;}list.innerHTML=shown.map(job=>{const rule=ruleFor(job.system);return '<div class="scout-job-admin-row" data-scout-job-system="'+esc(job.system)+'"><strong>'+esc(job.system)+'<small style="display:block;color:#7f969c;font-weight:500;margin-top:3px">'+esc(job.status.replaceAll('_',' ').toUpperCase())+' · tick '+esc(job.cycle?.tickConfiguredTime||'19:00')+' CT</small></strong><label><span>Bonus M Cr</span><input type="number" min="0" max="100000" step="1" data-scout-job-bonus value="'+esc(rule.bonusMillions||0)+'"></label><label><span>Priority reason</span><input type="text" maxlength="240" data-scout-job-reason value="'+esc(rule.reason||'')+'" placeholder="e.g. Conflict / expansion watch"></label><label><span>Bonus behavior</span><select data-scout-job-once><option value="once" '+(rule.bonusOnce!==false?'selected':'')+'>Next rewarded scout only</option><option value="persistent" '+(rule.bonusOnce===false?'selected':'')+'>Every cycle until cleared</option></select></label><div class="scout-job-actions"><label style="display:flex;align-items:center;gap:6px"><input type="checkbox" data-scout-job-enabled '+(rule.enabled!==false?'checked':'')+'> Enabled</label><button type="button" class="btn btn-secondary btn-compact" data-save-scout-job-system>SAVE</button></div></div>';}).join('');}
  async function load(){if(refresh)refresh.disabled=true;if(status)status.textContent='Refreshing Scout Job controls…';try{payload=await request();render();if(status)status.textContent='Loaded '+Number(payload?.summary?.systems||0).toLocaleString()+' active systems · default '+fmtCredits(payload.defaultRewardMillions||0);}catch(error){console.error('Could not load Scout Job admin',error);if(status)status.textContent='Scout Job controls unavailable · '+String(error.message||error);}finally{if(refresh)refresh.disabled=false;}}
  async function saveGlobal(value,label){if(saveDefault)saveDefault.disabled=true;if(resetDefault)resetDefault.disabled=true;if(status)status.textContent=label+' default Scout reward…';try{await request('POST',{action:'save-global',defaultRewardMillions:value});if(defaultInput)defaultInput.value=String(value);await load();}catch(error){if(status)status.textContent='Default reward not saved · '+String(error.message||error);}finally{if(saveDefault)saveDefault.disabled=false;if(resetDefault)resetDefault.disabled=false;}}
  saveDefault?.addEventListener('click',()=>saveGlobal(Number(defaultInput?.value)||0,'Saving'));resetDefault?.addEventListener('click',()=>saveGlobal(0,'Resetting'));
  list?.addEventListener('click',async event=>{const button=event.target.closest('[data-save-scout-job-system]');if(!button)return;const row=button.closest('[data-scout-job-system]'),system=row?.dataset.scoutJobSystem||'';if(!system)return;button.disabled=true;if(status)status.textContent='Saving '+system+' Scout reward…';try{await request('POST',{action:'save-system',system,enabled:Boolean(row.querySelector('[data-scout-job-enabled]')?.checked),bonusMillions:Number(row.querySelector('[data-scout-job-bonus]')?.value)||0,reason:row.querySelector('[data-scout-job-reason]')?.value||'',bonusOnce:row.querySelector('[data-scout-job-once]')?.value!=='persistent'});await load();}catch(error){console.error(error);if(status)status.textContent=system+' not saved · '+String(error.message||error);button.disabled=false;}});
  search?.addEventListener('input',()=>{page=1;render();});prev?.addEventListener('click',()=>{if(page>1){page--;render();}});next?.addEventListener('click',()=>{const pages=Math.max(1,Math.ceil(filteredJobs().length/PAGE_SIZE));if(page<pages){page++;render();}});refresh?.addEventListener('click',load);load();
})();