(() => {
 const list=document.querySelector('#glossary-list'); if(!list)return;
 const search=document.querySelector('#glossary-search'), category=document.querySelector('#glossary-category'), count=document.querySelector('#glossary-count'); let entries=[];
 const esc=(s='')=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
 function render(){const q=search.value.trim().toLowerCase(),cat=category.value; const shown=entries.filter(e=>(cat==='all'||e.category===cat)&&(!q||[e.term,e.expanded,e.definition,e.category].join(' ').toLowerCase().includes(q))); count.textContent=`${shown.length} of ${entries.length} terms`; list.innerHTML=shown.map(e=>`<article class="glossary-row" id="term-${esc(e.term.toLowerCase().replace(/[^a-z0-9]+/g,'-'))}"><div class="glossary-term"><strong>${esc(e.term)}</strong>${e.expanded?`<span>${esc(e.expanded)}</span>`:''}</div><p>${esc(e.definition)}</p><span class="glossary-category">${esc(e.category)}</span></article>`).join('')||'<p class="empty-state">No glossary terms match that search.</p>';}
 fetch('../../data/glossary.json',{cache:'no-store'}).then(r=>r.json()).then(d=>{entries=(d.entries||[]).sort((a,b)=>a.term.localeCompare(b.term)); [...new Set(entries.map(e=>e.category))].sort().forEach(c=>category.insertAdjacentHTML('beforeend',`<option value="${esc(c)}">${esc(c)}</option>`)); render();}).catch(()=>{list.innerHTML='<p class="empty-state">Glossary data could not be loaded.</p>';count.textContent='Unavailable';}); search.addEventListener('input',render);category.addEventListener('change',render);
})();
